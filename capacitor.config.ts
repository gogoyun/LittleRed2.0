import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sympasoft.littlered2',
  appName: 'littlered2',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
